<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.bidding.gstar.GstarApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.bidding.gstar.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Khela.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.bidding.gstar.ui.home.DetailViewActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Khela.Detail"/>

        <activity android:name="com.bidding.gstar.ui.adminlogin.ChangePassword"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Khela.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"/>

        <activity android:name="com.bidding.gstar.ui.slideshow.InsertValueActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Khela.NoActionBar"
            android:windowSoftInputMode="adjustResize"/>
    </application>

</manifest>